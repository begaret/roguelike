#version 330 core

in vec2 v_uv;
in vec3 v_fg;
in vec3 v_bg;

uniform sampler2D u_texture;

out vec3 frag_color;

void main()
{
	vec4 tex_color = texture(u_texture, v_uv);
	if (tex_color.x == 0) {
		frag_color = v_bg;
	} else {
		frag_color = v_fg;
	}
}

