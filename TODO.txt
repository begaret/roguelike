* add error handling
* fix & in text input
* redo popup/ui
* fix special characters in curses backend
// add SDL2/OpenGL backend
* add dll/plugin for backend libraries?
* rewrite OpenGL backend
* fix flashing in popups
* shorten paths in options

----- future -----
scrolling map
overworld

* regeneration
for every turn:
hp += regeneration-- * (vitality / 10.0f);
until:
regeneration == 0

